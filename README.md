# Crypto Data Exchange
A python GUI app to securely shuffle a deck of cards.
# Run
1. Install python3.9
2. Install dependencies `pip install -r requirements.txt`
3. Run file `src/ui_demo.py` (ex. `$ python src/ui_demo.py`)